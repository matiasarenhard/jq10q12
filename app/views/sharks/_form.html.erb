<%= form_with(model: shark, local: true) do |form| %>
  <% if shark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shark.errors.count, "error") %> prohibited this shark from being saved:</h2>

      <ul>
      <% shark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color, id: :shark_color %>
  </div>

  <div class="field">
    <%= form.label :fish_id %>
    <%= form.collection_select :fish_id, Fish.all, :id, :name, {} %>
    
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
